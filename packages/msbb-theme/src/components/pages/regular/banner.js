const Banner = (props) => {
        return(
                <div className="banner-search">
                        <div className="wrapper">
                                <h2>{props.title}</h2>
                        </div>
                </div>
        )
}

export default Banner;